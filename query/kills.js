var kills = q('kills'); // Monsters Killed
var ik = q('ik'); // Flowey (nk for spare; everyone else change the 'k' to 's')
var tk = q('tk'); // Toriel
var sk = q('sk'); // Sans
var pk = q('pk'); // Papyrus (pd for dated)
// Undyne (ud for befriended)
// Alphys (ad for dated)
